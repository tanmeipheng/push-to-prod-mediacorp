"""\nPytest test suite for the Data Sync Worker resilience logic.\n\nVerifies that the exponential backoff retry decorator correctly handles\nHTTP 429 Too Many Requests responses from the Partner API.\n"""\n\nimport pytest\nfrom unittest.mock import patch, MagicMock, call\nfrom requests.exceptions import HTTPError\n\n# ---------------------------------------------------------------------------\n# Helpers\n# ---------------------------------------------------------------------------\n\ndef _make_http_error(status_code: int) -> HTTPError:\n    """Build a requests.HTTPError with a mocked response for the given status."""\n    mock_response = MagicMock()\n    mock_response.status_code = status_code\n    error = HTTPError(response=mock_response)\n    return error\n\n\ndef _make_ok_response(data: list) -> MagicMock:\n    """Build a successful mock response that returns *data* from .json()."""
    mock_response = MagicMock()\n    mock_response.status_code = 200\n    mock_response.json.return_value = data\n    # raise_for_status should be a no-op for a 200\n    mock_response.raise_for_status.return_value = None\n    return mock_response\n\n\n# ---------------------------------------------------------------------------\n# Tests\n# ---------------------------------------------------------------------------\n\nclass TestSyncDataRetryOn429:\n\n    @patch("requests.get")\n    def test_succeeds_immediately_when_no_rate_limit(self, mock_get):\n        """sync_data returns data when the first request succeeds."""
        expected = [{"id": 1, "name": "Alice"}]\n        mock_get.return_value = _make_ok_response(expected)\n\n        from fixed_code import sync_data\n        result = sync_data()\n\n        assert result == expected\n        assert mock_get.call_count == 1\n\n    @patch("requests.get")\n    def test_retries_once_on_429_then_succeeds(self, mock_get):\n        """sync_data retries after a single 429 and returns data on success."""
        expected = [{"id": 2, "name": "Bob"}]\n\n        rate_limit_response = MagicMock()\n        rate_limit_response.status_code = 429\n        rate_limit_response.raise_for_status.side_effect = _make_http_error(429)\n\n        ok_response = _make_ok_response(expected)\n\n        mock_get.side_effect = [rate_limit_response, ok_response]\n\n        from fixed_code import sync_data\n        # Patch wait so tests run instantly\n        with patch("fixed_code.wait_exponential", return_value=lambda retry_state: 0):\n            result = sync_data()\n\n        assert result == expected\n        assert mock_get.call_count == 2\n\n    @patch("requests.get")\n    def test_retries_multiple_times_on_429_then_succeeds(self, mock_get):\n        """sync_data retries several times on consecutive 429s before succeeding."""
        expected = [{"id": 3, "name": "Carol"}]\n\n        def make_429_response():\n            r = MagicMock()\n            r.status_code = 429\n            r.raise_for_status.side_effect = _make_http_error(429)\n            return r\n\n        mock_get.side_effect = [\n            make_429_response(),\n            make_429_response(),\n            make_429_response(),\n            _make_ok_response(expected),\n        ]\n\n        from fixed_code import sync_data\n        with patch("fixed_code.wait_exponential", return_value=lambda retry_state: 0):\n            result = sync_data()\n\n        assert result == expected\n        assert mock_get.call_count == 4\n\n    @patch("requests.get")\n    def test_raises_after_max_attempts_exceeded(self, mock_get):\n        """sync_data raises HTTPError after exhausting all retry attempts."""
        def make_429_response():\n            r = MagicMock()\n            r.status_code = 429\n            r.raise_for_status.side_effect = _make_http_error(429)\n            return r\n\n        # Always return 429 — more than the max 6 attempts\n        mock_get.side_effect = [make_429_response() for _ in range(7)]\n\n        from fixed_code import sync_data\n        with patch("fixed_code.wait_exponential", return_value=lambda retry_state: 0):\n            with pytest.raises(HTTPError):\n                sync_data()\n\n        assert mock_get.call_count == 6  # stop_after_attempt(6)\n\n    @patch("requests.get")\n    def test_does_not_retry_on_non_429_http_error(self, mock_get):\n        """sync_data does NOT retry on non-429 HTTP errors (e.g. 500)."""
        server_error_response = MagicMock()\n        server_error_response.status_code = 500\n        server_error_response.raise_for_status.side_effect = _make_http_error(500)\n\n        mock_get.return_value = server_error_response\n\n        from fixed_code import sync_data\n        with pytest.raises(HTTPError):\n            sync_data()\n\n        # Should only be called once — no retry for 500\n        assert mock_get.call_count == 1\n\n    @patch("requests.get")\n    def test_does_not_retry_on_404(self, mock_get):\n        """sync_data does NOT retry on 404 Not Found."""
        not_found_response = MagicMock()\n        not_found_response.status_code = 404\n        not_found_response.raise_for_status.side_effect = _make_http_error(404)\n\n        mock_get.return_value = not_found_response\n\n        from fixed_code import sync_data\n        with pytest.raises(HTTPError):\n            sync_data()\n\n        assert mock_get.call_count == 1\n\n    @patch("requests.get")\n    def test_returns_correct_data_after_retry(self, mock_get):\n        """Verify the exact payload returned after a successful retry."""
        expected_records = [\n            {"id": 10, "name": "Dave"},\n            {"id": 11, "name": "Eve"},\n        ]\n\n        rate_limit_response = MagicMock()\n        rate_limit_response.status_code = 429\n        rate_limit_response.raise_for_status.side_effect = _make_http_error(429)\n\n        mock_get.side_effect = [rate_limit_response, _make_ok_response(expected_records)]\n\n        from fixed_code import sync_data\n        with patch("fixed_code.wait_exponential", return_value=lambda retry_state: 0):\n            result = sync_data()\n\n        assert result == expected_records\n        assert len(result) == 2\n